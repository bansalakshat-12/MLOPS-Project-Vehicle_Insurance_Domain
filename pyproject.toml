[project]
name = "src"
version = "0.0.1"
description = "An MLOPS Project for productionizing models"
authors = [{ name = "Akshat Bansal", email = "akshatbansal217@gmail.com" }]

[tool.setuptools]
packages = { find = {} }

[tool.setuptools.dynamic]
dependencies = { file = ["requirements.txt"] }
